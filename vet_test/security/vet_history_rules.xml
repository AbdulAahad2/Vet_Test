<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="rule_vet_animal_history_service_all" model="ir.rule">
            <field name="name">All users can access wizard services</field>
            <field name="model_id" ref="model_vet_animal_history_service"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
        </record>

        <record id="rule_vet_animal_history_line_all" model="ir.rule">
            <field name="name">All users can access wizard lines</field>
            <field name="model_id" ref="model_vet_animal_history_line"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
        </record>

        <record id="rule_vet_animal_history_wizard_all" model="ir.rule">
            <field name="name">All users can access wizard</field>
            <field name="model_id" ref="model_vet_animal_history_wizard"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
        </record>

    </data>
</odoo>
