<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- SIMPLE: Vet can create invoices -->
        <record id="invoice_analytic_rule" model="ir.rule">
            <field name="name">Vet: Can Create Invoices</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="domain_force">[('move_type', 'in', ('out_invoice', 'out_refund'))]</field>
            <field name="groups" eval="[(4, ref('vet_test.group_vet_limited_user')), 
                                       (4, ref('vet_test.group_vet_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>
    </data>
</odoo>
